const termsConfig = {
  serviceVersion: process.env.SERVICE_TERMS_VERSION,
  privacyVersion: process.env.PRIVACY_TERMS_VERSION,
  locationVersion: process.env.LOCATION_TERMS_VERSION,
};

export default termsConfig;
